type solToUsd {
    price: Float
}

type Query {
    solToUsd(price: Float!): solToUsd
        @rest(
            endpoint: "https://price.jup.ag/v4/price?ids=SOL"
            ecmascript: """
            function transformREST(s) {
                let out = JSON.parse(s).data;

                if (out.price) {
                    out = { price: out.price * get("price") };
                }

                return JSON.stringify(out);
            }
            """
        )
}
